# BMS-3927 — Product search & filtering (category, brand, pack size)

> **ID**: `CAT-3927` · **Domain**: Catalog & Discovery (`ecom-catalog`) · **Jira**: https://ohanafy.atlassian.net/browse/BMS-3927
> **Status** (live 2026-06-11, 4th pull): In Progress · **MANUAL — Alvaro is developing this ticket himself. NOT in the orchestration queue.** Agents: treat his branch as a conflict source for ecomShop/navigationMenu search code.
> **Branch**: `BMS-3927-<slug>` (cut from main) · **Worktree**: _worker fills in absolute path as first action_


## Scope report

_Scoped 2026-06-11 by the scope fleet against the develop-only tree; the main merge (a395fe49) landed afterward — footprint may be stale, re-verify paths._

- **AC grade**: C · **Risk**: high
- **Packages**: OHFY-eCommerce-UI
- **Depends on**: BMS-3930 (linked 'is blocked by' + 'relates to'; status Done), Implicit: the ecom-to-split branch merge/package split work — BMS-3927's completed scope and all target files live on origin/ecom-to-split, not develop

**Approach**: Add tokenized fuzzy search to the Gulf ecommerce storefront: build a shared search-utility LWC module implementing multi-token matching with abbreviation/partial-term support (e.g. 'bud lt 12' -> Bud Light 12-pack) plus tiered relevance ranking (exact brand > partial pack-size) and special-character sanitization, then wire it into both ecomShop.checkSearchFilter (grid filter, currently naive substring includes on Name/SKU/brand) and navigationMenu.performSearch (nav dropdown, currently substring over concatenated fields). High risk for autonomous execution: every target file exists only on origin/ecom-to-split — not on develop — the fuzzy algorithm and ranking rules are underspecified, and verification requires an Experience Cloud org with seeded ecom catalog data that the standard dev pool does not provide.

**AC gaps:**
- Fuzzy-match algorithm unspecified: is 'lt' -> 'Light' an abbreviation dictionary, prefix-token matching, or edit-distance? Only one example ('bud lt 12') is given, so an agent cannot derive the general rule or build a complete test matrix.
- Pack-size source field undefined: '12' must match '12-pack' variants, but pack size is not a discrete field in the current search (checkSearchFilter only matches Name, ohfy__Item_Number__c, and Item_Type__r.Name). Is the agent matching against Name substrings, UOM, or a packaging field?
- Relevance ranking underspecified: 'exact brand matches first, then partial pack-size matches' gives only 2 tiers — ordering within a tier, handling of name/SKU matches, and whether ranking applies to the nav-bar dropdown (capped at 10 results) vs. the shop grid (currently unranked filter) are all unstated.
- Scope ambiguity: search exists in two places with separate implementations — ecomShop.checkSearchFilter (client-side grid filter) and navigationMenu.performSearch (dropdown). AC does not say whether fuzzy matching applies to both.
- 'No error thrown for special character input' has no defined expected behavior (strip chars? literal match? empty results?) and no list of characters to test.
- CRITICAL repo-state gap: all ecom code (OHFY-eCommerce-UI package, test-automation/tests/ecom/, orgScripts/e-commerce/) exists only on the origin/ecom-to-split branch — none of it is on develop or the current fable-project-as branch. An agent starting from develop will find zero target files. The ticket must state the working base branch.
- No existing Jest tests for ecomShop or navigationMenu (no __tests__ folders on ecom-to-split), so there is no regression harness; AC does not state required test coverage.
- 'Completed' items (warehouse scoping, pricing tier in results) are claimed done but unverifiable without org access — agent cannot confirm baseline behavior before changing search.

**Predicted files:**
- `OHFY-eCommerce-UI/force-app/main/default/lwc/ecomShop/ecomShop.js (exists ONLY on origin/ecom-to-split; checkSearchFilter ~line 3060 is the grid search match)`
- `OHFY-eCommerce-UI/force-app/main/default/lwc/navigationMenu/navigationMenu.js (exists ONLY on origin/ecom-to-split; performSearch ~line 1172 is the nav-bar search)`
- `NEW: OHFY-eCommerce-UI/force-app/main/default/lwc/ecomSearchUtils/ecomSearchUtils.js (shared tokenized fuzzy-match + ranking service module, imported by both components)`
- `NEW: OHFY-eCommerce-UI/force-app/main/default/lwc/ecomSearchUtils/__tests__/ecomSearchUtils.test.js`
- `NEW: OHFY-eCommerce-UI/force-app/main/default/lwc/ecomShop/__tests__/ecomShop.test.js (no existing tests for either component)`
- `NEW: test-automation/tests/ecom/product-search.spec.ts (test-automation/tests/ecom/ exists on ecom-to-split only)`

**Org setup needs:**
- Org with OHFY-eCommerce / OHFY-eCommerce-UI deployed — standard claim-dev pool orgs are built from develop and will NOT have the ecom package; need an ecom-to-split-based org (see origin/chore/ecom-org-setup branch)
- Experience Cloud storefront site published with retailer (Gulf-style) portal user; Playwright login creds for test-automation/tests/ecom specs (logged-in vs unauth projects)
- Ecom CMDT config: Ecom_Branding__mdt records, Configuration_Preference CMDT entries (EcomMinimumCaseQuantity, EcommerceShowQuantityAvailable, ecomOrderRepNotificationTemplate)
- Seed catalog data including Bud Light-style items with 12-pack variants, brand (Item_Type__c) and brand-family (Item_Line__c) hierarchy, pricelist + pricing tier, and warehouse assignment on the retailer Account (for warehouse-scoped results)
- Ecom permission sets / sharing for portal user product visibility (orgScripts/e-commerce/ scripts exist on ecom-to-split)

## Status log

## Decisions

## Escalations

## Done evidence
