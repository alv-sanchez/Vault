# BMS-4525 — Retailer credit terms — Ph 2: AR sync, invoice list & payment history (Gulf)

> **ID**: `CRED-4525` · **Domain**: Credit / AR (`ecom-credit`) · **Jira**: https://ohanafy.atlassian.net/browse/BMS-4525
> **Status** (live 2026-06-11): To Do · **Polish**: polished ✓ but OFF-SPRINT — lane deferred
> **Branch**: `BMS-4525-<slug>` (cut from main) · **Worktree**: _worker fills in absolute path as first action_


## Scope report

_Scoped 2026-06-11 by the scope fleet against the develop-only tree; the main merge (a395fe49) landed afterward — footprint may be stale, re-verify paths._

- **AC grade**: F · **Risk**: high
- **Packages**: OHFY-Data-Model, OHFY-PLTFM, OHFY-OMS, OHFY-OMS-UI
- **Depends on**: BMS-3930 (blocks this ticket; Done in Jira but its code is not on develop in this repo), Recommended-but-unfiled AR sync-mechanism spike with Gulf integration team (ticket says Ph 2 cannot start without it)

**Approach**: Three workstreams: (1) build an AR-to-Salesforce payment ingest landing Gulf AR payments into the existing Receipt__c object and keeping Invoice__c.Payment_Status__c accurate; (2) automate Account.Past_Due_Balance__c via a Schedulable batch mirroring the existing B_Account_AccountBalanceUpdater (sum Invoice__c.Total_Due__c where Payment_Due_Date__c < TODAY and Payment_Status__c != 'Paid', grouped by Customer__c — all fields verified to exist); (3) add an Outstanding Invoices list plus Last Payment line to the ecom Payment Status page via OMS_UI_Wrappers + a new global DTO. High risk for autonomous execution: the ticket explicitly gates itself on an unresolved external-integration spike, carries open design decisions instead of acceptance criteria, and its Ph 1 prerequisite (BMS-3930) is Done in Jira but absent from develop, leaving no Payment Status page or credit fields to build on in this repo.

**AC gaps:**
- No acceptance criteria section at all — the ticket is a story statement plus description plus five explicitly open refinement questions; the ticket itself says it 'cannot be started' until the Gulf integration team confirms the AR sync mechanism
- Workstream 1 (AR-to-Salesforce sync into Receipt__c) has no defined mechanism, cadence, auth, payload shape, or matching rules (how AR payments map to Invoice__c / Account__c); nothing in the repo references Receipt__c from Apex today, so this is greenfield integration work against an external system the agent cannot see
- Past_Due_Balance__c implementation approach is explicitly undecided (batch vs rollup vs trigger-driven) — open question #2; the agent would have to pick one without product sign-off
- Migration plan for Ph 1 manually-entered Past_Due_Balance__c values is an open question (#3) with no decision
- Ph 1 (BMS-3930) is marked Done in Jira but none of its artifacts exist on develop in this repo: no Account.Past_Due_Balance__c / Credit_Limit / Credit_Status / Credit_Last_Reviewed fields, no credit-summary or Payment Status page LWC, no test-automation/tests/ecom/ specs, no orgScripts/e-commerce/ — so the 'expand the Payment Status page' workstream has no component to expand here; Ph 1 code may live on an unmerged branch
- UX for the Outstanding Invoices section is loosely specified: no design reference, no empty-state/pagination/row-limit behavior, no definition of the past-due badge styling, no statement of how the section coexists with the Ph 1 summary card
- Open question #5 (invoice PDF download in scope or not) is unresolved
- No statement of how the storefront LWC is exposed to the Experience Cloud guest/retailer user (perm sets, sharing, with/without sharing context) — Receipt__c and Invoice__c FLS/sharing for community users is unaddressed
- Receipt__c field list in the ticket is slightly stale: actual fields include Amount_Applied__c, Unapplied_Amount_Formula__c, Parent_Receipt__c, Bank_Transaction__c but no Date__c semantics doc — 'most recent Receipt__c.Date__c' vs Status__c filter (which statuses count as posted?) is undefined

**Predicted files:**
- `OHFY-PLTFM/force-app/main/default/classes/batchJobs/account/B_Account_AccountBalanceUpdater.cls (reference precedent; may be touched)`
- `OHFY-PLTFM/force-app/main/default/classes/batchJobs/account/B_Account_AccountBalanceUpdater_T.cls`
- `OHFY-OMS-UI/force-app/main/default/classes/wrappers/OMS_UI_Wrappers.cls`
- `OHFY-Data-Model/force-app/main/default/objects/Receipt__c/ (existing object, fields verified: Account__c, Invoice__c, Amount__c, Date__c, Status__c, Entity__c)`
- `NEW: OHFY-Data-Model/force-app/main/default/objects/Account/fields/Past_Due_Balance__c.field-meta.xml (Ph 1 field not present on develop)`
- `NEW: OHFY-PLTFM/force-app/main/default/classes/batchJobs/account/B_Account_PastDueBalanceUpdater.cls + _T (mirrors B_Account_AccountBalanceUpdater)`
- `NEW: OHFY-OMS/force-app/main/default/classes/services/receipt/ or executables/ — AR payment ingest service + Receipt trigger service updating Invoice__c.Payment_Status__c (no Apex references Receipt__c today)`
- `NEW: OHFY-OMS/force-app/main/default/classes/triggerServices/receipt/ReceiptTriggerService.cls + trigger + Trigger_Configuration__mdt records`
- `NEW: OHFY-OMS-UI/force-app/main/default/lwc/<paymentStatus/outstandingInvoices> components (Ph 1 page does not exist in this repo)`
- `NEW: OHFY-OMS/force-app/main/default/classes/DTOs/<domain>/OutstandingInvoiceDTO.cls + _T per ADR-0007 (cross-package AuraEnabled shape)`

**Org setup needs:**
- Ecom-ready org via /ecom-setup skill — but the skill is marked DRAFT with TBD steps; orgScripts/e-commerce/ecom-perm-sets/ referenced by it is NOT committed on this branch
- Experience Cloud storefront site config (publication, retailer community user profile, self-registration) — partly TBD in ecom-setup skill
- Perm set / FLS grants for community retailer users on Invoice__c, Receipt__c, Account credit fields
- External credentials / Named Credential for Gulf's AR system (mechanism unknown — needs integration team spike)
- Scheduled-job registration for the new Past_Due_Balance batch (System.schedule, like the existing balance batch)
- Seed data for Receipt__c and unpaid/past-due Invoice__c records (none in data/sample-data-plan.json today) to demo the invoice list and Last Payment line
- Ph 1 metadata (Account credit fields, Payment Status page FlexiPage in org-metadata managed+scratch) must land first

## Status log

## Decisions

## Escalations

## Done evidence
